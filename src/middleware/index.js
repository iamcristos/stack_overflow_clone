import question from './question.middleware';
import user from './user.middleware';
import answer from './answer.middleware';

const middleware = { question, user, answer };

export default middleware;
